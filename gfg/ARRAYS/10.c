// rotation of the array

#include <stdio.h>
#include <stdlib.h>


int main(void) 
{
	int arr[] ={1,2,3,4,5,6};
    int temp [] =arr;
    int size= sizeof(arr)/sizeof(arr[0]);
    for(int i =0;i<size;i++)
    {
        if(arr[size-i]!=temp[size-i])
        {
            arr[]
        }
    }

	return 0;
}